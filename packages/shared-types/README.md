# packages/shared-types

Shared schema contracts between apps/api (TypeScript) and services/calc-engine (Python), generated
from a single OpenAPI specification so the two never drift.

Not yet implemented.
