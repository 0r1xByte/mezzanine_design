# docs

- `screenshots/`: current-state screenshots of `apps/web`, used in the root and `apps/web` READMEs.
  Regenerate with a real local stack running rather than hand-editing — see `apps/web`'s README for
  how to run everything.
- `domain-requirements.md` (pending): the fully answered domain brief, frozen as the spec Phase 1+
  is graded against (see PLAN.md Phase 0).
- `api-contracts.md` (pending): the Node/Python API boundary once the shared schema is defined.
